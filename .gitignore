node_modules/
dist/
coverage/
.env
content/
samples/
reports/
config/allowed-emails.json
config/access.json
config/goal-scenarios.seed.json
workers/**/.wrangler/
.wrangler/
config/backup-alerts.json
config/dev.json
.tmp/
.DS_Store

# Local-only portfolio / audit notes (not in repo)
docs/AUDIT*.md
docs/PUBLIC_READINESS.md

# Claude Code local settings — can accumulate secrets in permission-allowlist
# strings (API tokens pasted inline into commands). Never commit; this repo is public.
.claude/settings.local.json
